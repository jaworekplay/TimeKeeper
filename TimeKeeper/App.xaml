<Application x:Class="TimeKeeper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TimeKeeper"
             xmlns:vms="clr-namespace:TimeKeeper.VMs"
             xmlns:base="clr-namespace:TimeKeeper.Base"
             xmlns:enums="clr-namespace:TimeKeeper.Enums"
             xmlns:models="clr-namespace:TimeKeeper.Models"
             xmlns:converters="clr-namespace:TimeKeeper.Converters"
             xmlns:behaviours="clr-namespace:TimeKeeper.Behaviours"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Ditionaries/Templates.xaml"/>
                <ResourceDictionary Source="/Ditionaries/Colours.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Path x:Key="TimeKeeper.Path.Open" Width="16" Height="16"
                Stretch="Uniform"
                Fill="DarkSlateGray"
                Data="M3,3 L13,3 L13,13 L3,13 Z M5,5 L11,5 L11,11 L5,11 Z"/>
            <Path x:Key="TimeKeeper.Path.Close" Width="16" Height="16"
                Stretch="Uniform"
                Fill="Red"
                Data="M2,2 L14,14 M14,2 L2,14"
                Stroke="Red"
                StrokeThickness="2"/>
            <Path x:Key="TimeKeeper.Path.Save" Fill="#333" Stretch="Uniform" Width="16" Height="16"
              HorizontalAlignment="Center" VerticalAlignment="Center"
              Data="M2,0 L14,0 L16,2 L16,16 L0,16 L0,0 Z 
                    M4,0 L4,4 L12,4 L12,0 Z 
                    M4,6 L12,6 L12,14 L4,14 Z"/>
            <Path x:Key="TimeKeeper.Path.PopOut" Data="M10,0 L20,10 L10,20 L0,10 Z"
                  Fill="#FF007ACC"
                  Stretch="Uniform"
                  Width="24" Height="24"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
            <Path x:Key="TimeKeeper.Path.CheckGreen" Data="M 1,5 L 4,8 L 9,1" 
                  Stroke="Green" 
                  StrokeThickness="2" 
                  StrokeStartLineCap="Round" 
                  StrokeEndLineCap="Round" 
                  StrokeLineJoin="Round" 
                  Stretch="Uniform" 
                  Width="20" Height="20"/>
            <vms:MainViewModel x:Key="MainViewModel"/>
            <SolidColorBrush x:Key="ListBox.Static.Background" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="ListBox.Static.Border" Color="#FFABADB3"/>
            <SolidColorBrush x:Key="ListBox.Disabled.Background" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="ListBox.Disabled.Border" Color="#FFD9D9D9"/>
            <SolidColorBrush x:Key="EslScrollbarThumb" Color="#8b8b8b" />

            <converters:DateTimeToDayOfTheWeekConverter x:Key="DateTimeToDayOfTheWeekConverter"/>
            <converters:TodayToBoolConverter x:Key="TodayToBoolConverter"/>

            <Style TargetType="{x:Type ListBox}">
                <Setter Property="Background" Value="{DynamicResource TimeKeeper.Background}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource TimeKeeper.Foreground}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{DynamicResource TimeKeeper.Foreground}"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
                <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
                <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ListBox}">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1" SnapsToDevicePixels="true">
                                <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="false">
                                    <Setter Property="Background" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Background}"/>
                                    <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Border}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsGrouping" Value="true"/>
                                        <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="{x:Type Window}">
                <Setter Property="Content" Value="{Binding .}"/>
                <Setter Property="Title" Value="{Binding Title}"/>
                <Setter Property="Tag" Value="{Binding Identifier}"/>
                <Setter Property="Background" Value="{DynamicResource TimeKeeper.Background}"/>
                <!--<Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="WindowChrome.CaptionHeight" Value="34"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Window">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Row="1" Grid.ColumnSpan="4"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>-->
            </Style>
            <Style TargetType="{x:Type local:MainWindow}" BasedOn="{StaticResource {x:Type Window}}">
                <Setter Property="DataContext" Value="{StaticResource MainViewModel}"/>
            </Style>

            <Style x:Key="ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
                <Setter Property="Visibility" Value="Visible" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="true" />
                <Setter Property="IsTabStop" Value="false" />
                <Setter Property="Focusable" Value="false" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RepeatButton}">
                            <Border Background="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="true" />
                <Setter Property="IsTabStop" Value="false" />
                <Setter Property="Focusable" Value="false" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border
                        CornerRadius="3"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        Width="6" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ScrollBarThumbHor" TargetType="{x:Type Thumb}">
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="true" />
                <Setter Property="IsTabStop" Value="false" />
                <Setter Property="Focusable" Value="false" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Thumb}">
                            <Border
                                CornerRadius="3"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Height="6" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
                <Track
                    Name="PART_Track"
                    IsDirectionReversed="true">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb
                            Style="{StaticResource ScrollBarThumb}"
                            Margin="2,0,2,0"
                            Background="{DynamicResource EslScrollbarThumb}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </ControlTemplate>

            <ControlTemplate x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
                <Track
                    Name="PART_Track"
                    IsDirectionReversed="False">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb
                            Style="{StaticResource ScrollBarThumbHor}"
                            Margin="0,2,0,2"
                            Background="{DynamicResource EslScrollbarThumb}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </ControlTemplate>

            <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="true" />
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Width" Value="Auto" />
                        <Setter Property="Height" Value="10" />
                        <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
                    </Trigger>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="10" />
                        <Setter Property="Height" Value="Auto" />
                        <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="FavsScrollViewer" TargetType="{x:Type ScrollViewer}">
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter Grid.Column="1" />
                                <ScrollBar
                            Grid.Row="0"
                            Grid.Column="0"
                            Name="PART_VerticalScrollBar"
                            Value="{TemplateBinding VerticalOffset}"
                            Maximum="{TemplateBinding ScrollableHeight}"
                            ViewportSize="{TemplateBinding ViewportHeight}"
                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                                <ScrollBar
                            Name="PART_HorizontalScrollBar"
                            Orientation="Horizontal"
                            Grid.Row="1"
                            Grid.Column="1"
                            Value="{TemplateBinding HorizontalOffset}"
                            Maximum="{TemplateBinding ScrollableWidth}"
                            ViewportSize="{TemplateBinding ViewportWidth}"
                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ScrollViewer}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter Grid.Column="0" />
                                <ScrollBar
                            x:Name="PART_VerticalScrollBar"
                            Grid.Row="0"
                            Grid.Column="1"
                            Value="{TemplateBinding VerticalOffset}"
                            Maximum="{TemplateBinding ScrollableHeight}"
                            ViewportSize="{TemplateBinding ViewportHeight}"
                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                                <ScrollBar
                            x:Name="PART_HorizontalScrollBar"
                            Orientation="Horizontal"
                            Grid.Row="1"
                            Grid.Column="0"
                            Value="{TemplateBinding HorizontalOffset}"
                            Maximum="{TemplateBinding ScrollableWidth}"
                            ViewportSize="{TemplateBinding ViewportWidth}"
                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBox" x:Key="TimeKeeper.TextBox">
                <Setter Property="Margin" Value="0,0,5,0"/>
                <Setter Property="Background" Value="{DynamicResource TimeKeeper.Background}"/>
                <Setter Property="Foreground" Value="{DynamicResource TimeKeeper.Foreground}"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TimeKeeper.TextBox}"/>
            <Style TargetType="TextBlock" x:Key="TimeKeeper.TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TimeKeeper.Foreground}"/>
                <Setter Property="Margin" Value="0,0,5,0"/>
            </Style>
            <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TimeKeeper.TextBlock}"/>
            <Style TargetType="TextBlock" x:Key="TimeKeeper.TextBlock.Active" BasedOn="{StaticResource TimeKeeper.TextBlock}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                        <Setter Property="FontWeight" Value="DemiBold"/>
                        <Setter Property="FontSize" Value="18"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="ListView">
                <Setter Property="Background" Value="{DynamicResource TimeKeeper.Background}"/>
                <Setter Property="BorderThickness" Value="0, 0, 0.5, 0.5"/>
            </Style>
            <Style TargetType="Button" x:Key="TimeKeeper.RoundedButton">
                <Setter Property="Background" Value="{DynamicResource TimeKeeper.Background}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                Padding="2" 
                                BorderBrush="{DynamicResource TimeKeeper.Foreground}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter Margin="2"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    RecognizesAccessKey="True" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FF0090FF"/>
                        <Setter Property="Foreground" Value="WhiteSmoke"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource TimeKeeper.RoundedButton}"/>
            <Style TargetType="Button" x:Key="TimeKeper.RedRoundedButton" BasedOn="{StaticResource TimeKeeper.RoundedButton}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="Red"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Button" x:Key="TimeKeper.GreenRoundedButton" BasedOn="{StaticResource TimeKeeper.RoundedButton}">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="ForestGreen"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <DataTemplate DataType="{x:Type vms:JiraTaskDetailsViewModel}">
                <Grid>
                    <Grid.Resources>
                        <CollectionViewSource x:Key="JiraTask.TimeSpans.CVS" Source="{Binding Task.TimeSpans}">
                            <CollectionViewSource.GroupDescriptions>
                                <PropertyGroupDescription PropertyName="StartDate" Converter="{StaticResource DateTimeToDayOfTheWeekConverter}"/>
                            </CollectionViewSource.GroupDescriptions>
                            <CollectionViewSource.SortDescriptions>
                                <scm:SortDescription PropertyName="StartDate" Direction="Descending"/>
                            </CollectionViewSource.SortDescriptions>
                        </CollectionViewSource>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <ListView 
                        Grid.RowSpan="4" 
                        Grid.ColumnSpan="2" 
                        SelectionMode="Extended"
                        ItemsSource="{Binding Source={StaticResource JiraTask.TimeSpans.CVS}}" d:ItemsSource="{d:SampleData ItemCount=5}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding RecalculateTimeCommand}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="{Binding Name, Mode=OneWay, Converter={StaticResource TodayToBoolConverter}}">
                                                        <Expander.Header>
                                                            <DockPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                                                <TextBlock Text="{Binding ItemCount, StringFormat='({0})'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                                            </DockPanel>
                                                        </Expander.Header>
                                                        <Expander.Content>
                                                            <ItemsPresenter/>
                                                        </Expander.Content>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ListView.GroupStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="models:TimeKeeperTimeSpan">
                                <StackPanel>
                                    <TextBlock Text="{Binding StartDate, StringFormat={}Started on: {0:g}}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding Time, StringFormat='Time spent: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding TimeSpanType, StringFormat='Type: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding AdditionalInformation, StringFormat='Notes: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                    <TextBox IsReadOnly="True" Text="{Binding TotalWorkTime, StringFormat={}Total time: {0} }" Grid.Column="2" VerticalAlignment="Top" HorizontalContentAlignment="Left"/>
                    <StackPanel Grid.Column="2" Grid.Row="1">
                        <!--<RadioButton Content="All" Command="{Binding SelectTasksByTypeCommand}"/>-->
                        <RadioButton Content="Breaks only" Command="{Binding SelectTasksByTypeCommand}">
                            <RadioButton.CommandParameter>
                                <enums:ETimeSpanType>Break</enums:ETimeSpanType>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton Content="Meetings only" Command="{Binding SelectTasksByTypeCommand}">
                            <RadioButton.CommandParameter>
                                <enums:ETimeSpanType>Meeting</enums:ETimeSpanType>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                        <RadioButton Content="Work only" Command="{Binding SelectTasksByTypeCommand}">
                            <RadioButton.CommandParameter>
                                <enums:ETimeSpanType>Work</enums:ETimeSpanType>
                            </RadioButton.CommandParameter>
                        </RadioButton>
                    </StackPanel>
                </Grid>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:MainViewModel}">
                <DataTemplate.Resources>
                    <ContextMenu x:Key="ContextMenu.Tasks">
                        <MenuItem Header="Save All" Command="{Binding Source={StaticResource MainViewModel}, Path=SaveCommand}" Icon="{DynamicResource TimeKeeper.Path.Save}"/>
                        <MenuItem Header="Archive" Icon="{DynamicResource TimeKeeper.Path.Close}" Command="{Binding Source={StaticResource MainViewModel}, Path=ArchiveTaskCommand}" CommandParameter="{Binding .}"/>
                        <MenuItem Header="Add break" Icon="{DynamicResource TimeKeeper.Path.Open}" Command="{Binding Source={StaticResource MainViewModel}, Path=AddTimeSpanCommand}" CommandParameter="{Binding .}"/>
                    </ContextMenu>
                    <CollectionViewSource x:Key="TimeKeeper.MainViewModel.SelectedTasks.CVS" Source="{Binding Source={StaticResource MainViewModel}, Path=SelectedTask.TimeSpans}">
                        <CollectionViewSource.GroupDescriptions>
                            <PropertyGroupDescription PropertyName="StartDate" Converter="{StaticResource DateTimeToDayOfTheWeekConverter}" />
                        </CollectionViewSource.GroupDescriptions>
                        <CollectionViewSource.SortDescriptions>
                            <scm:SortDescription PropertyName="StartDate" Direction="Descending"/>
                        </CollectionViewSource.SortDescriptions>
                    </CollectionViewSource>
                    <CollectionViewSource x:Key="TimeKeeper.MainViewModel.MainTasks" Source="{Binding Source={StaticResource MainViewModel}, Path=Tasks}">
                        <CollectionViewSource.GroupDescriptions>
                            <PropertyGroupDescription PropertyName="ModifiedDate"/>
                        </CollectionViewSource.GroupDescriptions>
                        <CollectionViewSource.SortDescriptions>
                            <scm:SortDescription PropertyName="ModifiedDate" Direction="Descending"/>
                        </CollectionViewSource.SortDescriptions>
                    </CollectionViewSource>
                </DataTemplate.Resources>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Command="{Binding CreateTaskCommand}" Content="{Binding CreateTaskCommand.Text, FallbackValue='Create'}"/>
                    <Button Command="{Binding SaveCommand}" Content="{Binding SaveCommand.Text, FallbackValue='Save'}" Grid.Column="1"/>
                    <Button Command="{Binding LoadSavedTasksCommand}" Content="{Binding LoadSavedTasksCommand.Text, FallbackValue='Load Saved Task'}" Grid.Column="2"/>
                    <Button Command="{Binding OpenSavedFilesCommand}" Content="{Binding OpenSavedFilesCommand.Text, FallbackValue='Open Files'}" Grid.Column="3"/>
                    <Button Command="{Binding ViewDayActivityCommand}" Content="{Binding ViewDayActivityCommand.Text, FallbackValue='View Day Activity'}" Grid.Column="4"/>
                    <TextBlock Text="{Binding TimeRunning, StringFormat='Time Running: {0}'}" Grid.Column="5" Style="{StaticResource TimeKeeper.TextBlock}"/>
                    <ListView 
                        ItemsSource="{Binding Tasks}" 
                        SelectedItem="{Binding SelectedTask}" 
                        Grid.Row="1" 
                        Grid.RowSpan="3" 
                        Grid.ColumnSpan="4" 
                        d:ItemsSource="{d:SampleData ItemCount=5}">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="ContextMenu" Value="{StaticResource ContextMenu.Tasks}"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="models:JiraTask">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" UndoLimit="10" MinWidth="90">
                                        <i:Interaction.Behaviors>
                                            <behaviours:FocusOnLoaded/>
                                        </i:Interaction.Behaviors>
                                    </TextBox>
                                    <TextBlock Text="{Binding TotalTime, StringFormat='Total time spent: {0}'}" Grid.Column="1" Style="{StaticResource TimeKeeper.TextBlock.Active}"/>
                                    <Button Content="Start" 
                                        Grid.Column="2" 
                                        Command="{Binding Source={StaticResource MainViewModel}, Path=StartTimerCommand}" 
                                        CommandParameter="{Binding .}"
                                        Margin="5,0"
                                        Style="{StaticResource TimeKeper.GreenRoundedButton}"/>
                                    <Button Content="Stop" 
                                        Command="{Binding Source={StaticResource MainViewModel}, Path=StopTimerCommand}" 
                                        CommandParameter="{Binding .}"
                                        Grid.Column="3" 
                                        Style="{StaticResource TimeKeper.RedRoundedButton}"/>
                                    <Button 
                                        Grid.Column="4"
                                        Margin="5,0"
                                        Content="Show Details" 
                                        Command="{Binding Source={StaticResource MainViewModel}, Path=PopOutCommand}"
                                        CommandParameter="{Binding .}"/>
                                    <!--<TextBox Text="{Binding ReminderDate}" Grid.Column="4"/>-->
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <ListView ItemsSource="{Binding Source={StaticResource TimeKeeper.MainViewModel.SelectedTasks.CVS}}" 
                              d:ItemsSource="{d:SampleData ItemCount=5}"
                              Grid.Row="1" 
                              Grid.Column="4" 
                              Grid.ColumnSpan="2" 
                              Grid.RowSpan="3">
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.ContainerStyle>
                                    <Style TargetType="{x:Type GroupItem}">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                                    <Expander IsExpanded="{Binding Name, Mode=OneWay, Converter={StaticResource TodayToBoolConverter}}">
                                                        <Expander.Header>
                                                            <DockPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                                                <TextBlock Text="{Binding ItemCount, StringFormat='({0})'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                                            </DockPanel>
                                                        </Expander.Header>
                                                        <Expander.Content>
                                                            <ItemsPresenter/>
                                                        </Expander.Content>
                                                    </Expander>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </GroupStyle.ContainerStyle>
                            </GroupStyle>
                        </ListView.GroupStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:TimeKeeperTimeSpan}">
                                <StackPanel>
                                    <TextBlock Text="{Binding StartDate, StringFormat={}Started on: {0:g}}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding Time, StringFormat='Time spent: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding TimeSpanType, StringFormat='Type: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                    <TextBlock Text="{Binding AdditionalInformation, StringFormat='Notes: {0}'}" Style="{StaticResource TimeKeeper.TextBlock}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </DataTemplate>
            <DataTemplate DataType="{x:Type base:MessageBoxViewModel}">
                <TextBlock Text="{Binding Message}" VerticalAlignment="Top" HorizontalAlignment="Center"/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:AddTimeSpanViewModel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Time taken:"/>
                    <TextBox Text="{Binding TimeSpan.Time}" Grid.Column="1" Grid.ColumnSpan="4"/>
                    <TextBlock Text="Type:" Grid.Row="1"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" ItemsSource="{Binding Types}" SelectedItem="{Binding TimeSpan.TimeSpanType}"/>
                    <TextBlock Text="Notes:" Grid.Row="2"/>
                    <TextBox 
                        Text="{Binding TimeSpan.AdditionalInformation, UpdateSourceTrigger=PropertyChanged}" 
                        Grid.Row="2" 
                        Grid.Column="1" 
                        Grid.ColumnSpan="4" 
                        Grid.RowSpan="2" 
                        AcceptsReturn="True"/>
                    <Button Content="OK" Grid.Row="4" Grid.Column="4" Command="{Binding CloseCommand}"/>
                </Grid>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vms:DayActivityViewModel}">
                <DataTemplate.Resources>
                    <DataTemplate DataType="{x:Type models:HourContentModel}">
                        <TextBlock Text="{Binding Title}" MinHeight="100" Background="Red"/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:HourSlotModel}">
                        <Grid MinWidth="200">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding HourText}"/>
                            <ItemsControl ItemsSource="{Binding Entries}" Grid.Row="1" Grid.ColumnSpan="3"/>
                        </Grid>
                    </DataTemplate>
                </DataTemplate.Resources>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.2*"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <ItemsControl ItemsSource="{Binding Tasks}">
                        <ItemsControl.ItemContainerStyle>
                            <Style>
                                <Setter Property="Control.MinHeight" Value="100"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="models:JiraTask">
                                <TextBlock Text="{Binding Title}" MinHeight="100"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ListBox Grid.Column="1" ItemsSource="{Binding Hours}" d:ItemsSource="{d:SampleData ItemCount=5}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
