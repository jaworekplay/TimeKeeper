<UserControl x:Class="TimeKeeper.Design.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeKeeper.Design"
             xmlns:vms="clr-namespace:TimeKeeper.VMs"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:TimeKeeper.Converters"
             xmlns:models="clr-namespace:TimeKeeper.Models"
             xmlns:enums="clr-namespace:TimeKeeper.Enums"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vms:DayActivityViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:DateTimeToDayOfTheWeekConverter x:Key="DateTimeToDayOfTheWeekConverter"/>
        <converters:TodayToBoolConverter x:Key="TodayToBoolConverter"/>
        <DataTemplate DataType="{x:Type models:HourSlotModel}">
            <Grid MinWidth="200">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding HourText}"/>
                <ContentControl Content="{Binding .}" Grid.Row="1" Grid.ColumnSpan="3"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ItemsControl ItemsSource="{Binding Tasks}">
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Control.MinHeight" Value="100"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="models:JiraTask">
                    <Border CornerRadius="2" MinHeight="100">
                        <TextBlock Text="{Binding Title}"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ListBox Grid.Column="1" ItemsSource="{Binding Hours}" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
